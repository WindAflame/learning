
void main() {
  while(cond) {}
  do {} while(cond)
  for (var, cond, inc) {}
  for (var in enum) {}
  var.forEach(print)
  var.forEach((v) => print(v))
  switch() {
    case :
        break;
    default:
        break;
  }

  List<String> value = List<String>(1);
  value[0] = "v";
  value.first
  value.isEmpty
  value.firstWhere()
  Map json;
}